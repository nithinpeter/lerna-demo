{
    "private": true,
    "devDependencies": {
        "lerna": "^2.11.0"
    },
    "useWorkspaces": true,
    "workspaces": ["packages/*"],
    "scripts": {
        "release":
            "lerna publish --conventional-commits --changelog-preset angular-bitbucket --skip-npm"
    }
}
